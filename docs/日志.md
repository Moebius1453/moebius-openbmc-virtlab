2026.9.6 今天搭建一个基于openbmcdocx的项目骨架
一个普通的读取传感器的任务

2026.9.7 今天加入method让meminfo服务至少可以被refresh读取并且呼叫

2026.9.8 今天弄了一个memwatcher服务 监听并且在内存波动大于10mb时在journalctl里面输出日志

2026.9.9 发现自己只是在造轮子
注册memwatcher到总线监听 虽然说bmc自带这种基础功能但是还是尝试写一个好了
const vtable_t MemoryStats::_vtable[] = {
    vtable::start(),

    vtable::method("Refresh", ..., _callback_Refresh),

    vtable::property("MemTotal", ...,
                     _callback_get_MemTotal,      // 谁来处理「读」
                     _callback_set_MemTotal,      // 谁来处理「写」
                     vtable::property_::emits_change),   // ← 见下
    vtable::property("MemAvailable", ..., _callback_get_MemAvailable,
                     _callback_set_MemAvailable,
                     vtable::property_::emits_change),
    vtable::end()
}; 
busctl get-property
  → 线上出现 MethodCall 消息
    → libsystemd 收到，查 _vtable
      → 找到 MemTotal 那一行 → 调 _callback_get_MemTotal
        → o->memTotal()  ← 读缓存
          → 回包 MethodReturn，字节流原路返回
好像很关键的东西 记下来

2026.9.10
让meminfo使用标准接口 //不对 系统已经有使用标准接口的服务了

2026.9.16
把meminfo的接口标准化而不是继续抱着自己捏出来的接口

2026.9.18
在redfish上面写了第一个在bmc网页上可见的helloworld程序

2026.9.19
尝试接入redfish 把自己的meminfo借用sensor这个接口塞进去

2026.9.22
准备弄healthreporter 就是叠叠乐 把bmc本身和服务器状态打包塞进一个日志里面

2026.9.24
准备落地healthreporter

// ① 读字符串属性：把你 meminfoweb 的 readMetric 改一下类型即可
std::string readStrProp(const std::string& service, const std::string& path,
                        const std::string& iface, const std::string& prop)
{
    auto method = bus_.new_method_call(service, path, "org.freedesktop.DBus.Properties", "Get");
    method.append(iface, prop);
    auto reply = bus_.call(method);
    std::variant<std::string> v;
    reply.read(v);
    return std::get<std::string>(v);
}
// uint64 版本同理：std::variant<uint64_t>（LastRebootTime 用它）

// ② 枚举取尾段：rfind=从右找最后一个点，substr 从它后面切到结尾
static std::string tail(const std::string& s) { return s.substr(s.rfind('.') + 1); }
// "xyz.openbmc_project.State.Host.HostState.Off" → "Off"

// ③ epoch 毫秒 → uptime 秒
auto nowMs = std::chrono::duration_cast<std::chrono::milliseconds>(
                 std::chrono::system_clock::now().time_since_epoch()).count();
uint64_t uptimeSec = (nowMs - lastReboot) / 1000;

// ④ journal 输出：stdout 会被 systemd 自动收进 journal
std::cout << "[HealthReporter] level=" << level << " uptime=" << uptimeSec << "s ..." << std::endl;
// std::endl = 换行 + flush，不然可能攒着不吐出来 随手的记录
且深刻认识到了本质上都是差不多的开发流程 重点是自己缺乏写什么和怎么写好的经验
还有就是 做到什么程度算bmc开发呢
